  (- improve datasets import possibilities, issue)
  (- event delay, min duration, 2 issues)
  (- add and improve ui_logger logs, issue)

isot:
  (- tse should not look like a spider, so always compute longer, but save tmin and tmax that are used to crop?)
  (- add statistics, issue)
  (- add tests, should try to at least figure out a strategy)
